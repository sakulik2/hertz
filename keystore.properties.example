# 复制为 keystore.properties（不入库）后填入真实值。
# 四项缺任意一项，release 会退回未签名构建，不会报错 —— 见 app/build.gradle.kts。
#
# storeFile 用正斜杠写绝对路径：.properties 里反斜杠是转义符，
# C:\Users\me\key.jks 会被解析成 C:Usersmekey.jks；也不要加引号，引号不会被剥掉。
storeFile=C:/Users/you/.jks/hertz-release.jks
storePassword=
keyAlias=
keyPassword=
