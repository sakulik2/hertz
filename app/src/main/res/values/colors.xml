<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        窗口背景色（亮色）。必须与 ui/theme/Color.kt 的 BackgroundLight 一致：
        themes.xml 用它做 windowBackground，Compose 用它做 background，
        两者不一致会在启动时出现一帧色差闪烁。
        深色对应值见 values-night/colors.xml。
    -->
    <color name="hertz_window_background">#FFF7F8FA</color>
</resources>
